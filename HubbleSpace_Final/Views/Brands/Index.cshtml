@model IEnumerable<HubbleSpace_Final.Entities.Brand>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Brand_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Brand_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Brand">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Brand">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Brand">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
