@model IEnumerable<HubbleSpace_Final.Entities.Size>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.size)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.color_Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.size)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.color_Product.Color_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Size_Product">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Size_Product">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Size_Product">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
