@model IEnumerable<HubbleSpace_Final.Entities.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Object)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Category_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Object)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Categorie">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Categorie">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Categorie">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
